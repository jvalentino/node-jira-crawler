
function generate(outFile, projectsWithStories, users) {
    // figure out the columns for excel
    const columns = [];

    for (let project of projectsWithStories) {
        
    }
}

module.exports = {
  generate: generate,
};